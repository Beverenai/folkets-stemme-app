import OnboardingFlow, { checkOnboardingCompleted } from '@/components/onboarding/OnboardingFlow';

export default function Onboarding() {
  return <OnboardingFlow />;
}

export { checkOnboardingCompleted };
